import { ProjectItemDirectory, ProjectModel } from './nod4japi/project';

const main = () => {
  if (process.argv.length != 2) {
    console.log('OK');
  } else {
    console.log('NG');
  }
};

main();
